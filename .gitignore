# OS
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*
!.env.example
*.log

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Packaging / build
build/
dist/
*.egg-info
.eggs/
*.egg
pip-wheel-metadata/
# This project locks via pyproject (hatchling/hatch-vcs) and installs with pip/pipx;
# a uv.lock is a local artifact, not part of the build — don't track it.
uv.lock

# Virtualenvs
.venv/
venv/
env/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.tox/
.nox/

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Editors / IDEs
.idea/
.vscode/
*.swp

# Claude Code per-user local settings (the shared .claude/settings.json is tracked)
.claude/settings.local.json
